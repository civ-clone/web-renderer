{"version":"0.1.0@24c2770","date":"2026-09-26T07:18:40.000Z","localChanges":["chore: update civ1-unit, simple-ai-client and base-unit-action-goto"],"externalChanges":{"base-unit-action-goto":{"status":"updated","log":["Where a tile offers more than one Move-type action, moveAlongPath now prefers an action whose class is exactly Move while the unit is only passing through, and takes the first Move-type action as before on the last tile of the route or with the unit's last move.","Only aircraft get both kinds on one tile (civ1-unit offers LandAircraft and Move on friendly cities and Carriers), so this lets a GoTo fly over them instead of landing and ending the turn, while still landing at the destination, or on a Carrier reached with the last move rather than crashing over it (civ-clone/web-renderer#82)."]},"civ1-unit":{"status":"updated","log":["Move is now offered to Air units on tiles where they could land, and the land-aircraft action is registered before move so LandAircraft stays the first action there: the arrow keys and SimpleAIClient's neighbour scoring still land. A GoTo (base-unit-action-goto's moveAlongPath) and the frontend's action menu can take the plain Move to fly over instead (civ-clone/web-renderer#82).","Tests cover the action order, flying over a city and a Carrier with a plain Move, and GoTo routes through cities, ending on a Carrier, and reaching a Carrier with the last move. base-unit-action-goto is added as a devDependency for those."]},"simple-ai-client":{"status":"updated","log":["aircraftCanReturn counts the player's Carriers with room as somewhere to get back to, as well as its cities.","An aircraft is deferred (setWaiting) while any of the player's Carriers that could carry it still have moves, so it plans against the Carrier's end-of-turn position.","preProcessTurn wakes the player's aircraft aboard its Carriers: Stowed never clears itself, so a landed aircraft would otherwise never fly again.","Path-following prefers a plain Move while passing through, as base-unit-action-goto's moveAlongPath does, so a path doesn't land in a city on the way (civ-clone/web-renderer#82).","carriersFor required hasCapacity(), which counts the aircraft's own weight while it is still aboard, so a full Carrier was never a return point for its own aircraft. With no city in reach every move scored -1 and the aircraft never took off. A Carrier whose cargo includes the aircraft now counts (review on civ-clone/simple-ai-client#3)."]}}}